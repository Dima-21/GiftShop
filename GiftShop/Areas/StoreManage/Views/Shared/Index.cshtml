@model GoodsListViewModel
<div>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">@Html.DisplayNameFor(model => model.Goods.First().Code)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.Goods.First().Image)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.Goods.First().Name)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.Goods.First().Price)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (GoodsViewModel item in Model.Goods)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td><img src="~/goods_image/@Url.Content(item.Image)" alt="Image" height="48" width="48"></td>
                    <td>@item.Name</td>
                    <td>@item.Price</td>
                </tr>
            }
        </tbody>
    </table>
</div>
